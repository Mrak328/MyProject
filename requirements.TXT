# FastAPI фреймворк
fastapi==0.104.1

# ASGI сервер для запуска
uvicorn[standard]==0.24.0

# Работа с PostgreSQL
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
asyncpg==0.29.0

# Валидация данных и переменные окружения
pydantic==2.5.0
pydantic-settings==2.1.0
python-dotenv==1.0.0

# Для работы с датами и временем
python-dateutil==2.8.2

# Для загрузки файлов (фото)
python-multipart==0.0.6

# Для работы с изображениями (опционально)
pillow==10.1.0

# Для CORS (уже встроен в fastapi, но указываем зависимость)
# (не требуется отдельной установки)

# Дополнительные утилиты
alembic==1.12.1  # для миграций БД (опционально)